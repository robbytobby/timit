require 'spec_helper'

describe UsersHelper do
  describe "human_roles" do
    #it "sets up an array for collection select with translated role and db-role" do
    #  User.roles.each do |role|
    #    helper.human_roles.should include([t("activerecord.attributes.user.#{role}"), role])
    #  end
    #end
  end
end

