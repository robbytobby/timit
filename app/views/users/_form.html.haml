= semantic_form_for @user do |f|
  = f.inputs do
    = f.input :email
    //= f.input :approved, :as => :radio
  = f.buttons do
    = f.commit_button
    %li.button
      = link_to t('formtastic.actions.cancel'), users_path
    
